/**
 * Represents the current state of the select dropdown
 */
export type IsSelectState = 'opened' | 'closed';

/**
 * Whether the is-select renders inline or in dropdown mode
 */
export type IsSelectType = 'inline' | 'dropdown';
